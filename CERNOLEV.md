```bash
cd Documents
mkdir eval

git init

git remote add origin https://github.com/ORyonRix/evalvladislavnicolasgabriel.git

git config user.name "ORyonRix"

git config user.email "cyborgtvtnt@gmail.com"

nano test.txt

git commit -m "test"

git push -u origin master

git branch mobile

git checkout mobile

mkdir css

cd css

nano mabile.css

../

git add *

git commit "mobile"

git push -u origin mobile

git branch css

git checkout css

mkdir cssnicolas

cd cssnicolas

git pull origin css

cd css

nano style.cc

git add style.cc

git commit -m "changement style"

git push -u origin master

```
